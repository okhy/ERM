import * as React from "react";
import { storiesOf } from "@storybook/react";
import MovieGridItem from "./MovieGridItem";

storiesOf("Components / MovieGrid / MovieGridItem", module).add("default", () => <MovieGridItem />);
